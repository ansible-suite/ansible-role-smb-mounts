smb_mount_mode: server_like

smb_mode_opts:
  server_like:
    - _netdev
    - nofail
    - x-systemd.requires=networking.service
    - x-systemd.after=networking.service

  workstation:
    - _netdev
    - noauto

smb_shares: []